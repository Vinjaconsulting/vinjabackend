# Vinja AI Backend

This is the backend server for Vinja AI, handling chat messages via OpenAI API.

## Setup
1. Add your OpenAI API key as an environment variable: `OPENAI_API_KEY`.
2. Deploy on Render or run locally with `npm install` and `node index.js`.